apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: test-vm-good
  namespace: default
  labels:
    cost-center: platform-eng
    team: infra
    environment: production
spec:
  runStrategy: Always
  template:
    spec:
      evictionStrategy: LiveMigrate
      domain:
        cpu:
          cores: 2
        memory:
          guest: 4Gi
        devices:
          disks: []
      volumes: []
