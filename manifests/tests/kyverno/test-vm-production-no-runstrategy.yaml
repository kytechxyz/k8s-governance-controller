apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: test-vm-prod-bad
  namespace: default
  labels:
    cost-center: platform-eng
    team: infra
    environment: production
spec:
  runStrategy: Halted
  template:
    spec:
      evictionStrategy: LiveMigrate
      domain:
        cpu:
          cores: 2
        memory:
          guest: 4Gi
        devices:
          disks: []
      volumes: []
